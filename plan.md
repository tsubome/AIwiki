# プロジェクト概要：次世代ローカルLLM統合プラットフォーム（仮）

## 1. 解決すべき課題（Core Pain Points）
現状のWeb上には、ローカルLLMに関する情報が整理されないまま散在しており、以下の問題が発生している。

* **情報の断片化:** Hugging Faceなどはあくまで「倉庫」であり、情報の体系化・構造化がなされていない。
* **AI検索の機能不全:** データが構造化されていないため、ChatGPTなどのLLMに最新モデルを尋ねても、正確な回答が得られない（AIが迷子になる）。
* **新着情報のノイズ:** ベースモデルとファインチューニング（FT）モデルが時系列でごちゃ混ぜに表示され、重要なリリースが埋もれる。
* **GGUFの乱立:** 同じモデルに対して複数の変換版（GGUF）が存在し、ユーザーがどれを使えばいいか判断できない。

## 2. プラットフォームのコンセプト
**「AIモデルのためのWikipedia」**
人間が見て分かりやすいだけでなく、**LLM自身がここを読み込めば「AIの進化の系譜」を正確に理解できるような、高度に構造化されたデータベース**を構築する。

### A. サイト構造とUI（情報の整理）
* **家系図（Genealogy）システム:**
    * 「ベースモデル（親）」と「FTモデル（子）」を明確に区別する。
    * 新着リストも「ベースモデルの新着」と「FTモデルの新着」を分ける。
    * ベースモデルのページを開くと、そこから派生したFTモデルのリストが階層的に表示される仕組みにする。
* **GGUFファイルの集約・提供:**
    * 乱立するGGUFリンクを放置せず、運営側で「推奨モデル」を提示、あるいは提供する。

### B. 検索システム（ユーザー体験）
* **対話型検索エンジン（AIコンシェルジュ）:**
    * 従来のキーワード検索ではなく、LLMをインターフェースに採用する。
    * ユーザーが「自分の環境（VRAM等）」「やりたいこと（日本語作文、英語翻訳等）」を自然言語で相談すると、最適なモデルを提案して返答する。

## 3. 運用・コンテンツ制作システム（CGM × AI）
Wikipediaのようなユーザー参加型メディアを目指すが、品質担保のためにAIと運営が介入する。

* **記事作成:**
    * ユーザー（有志）が検証記事やモデル紹介を投稿する。
    * 運営側のLLMも記事作成を行うほか、ユーザー投稿情報の「正しさ」の監視・ファクトチェック、フォーマット統一のサポートを行う。
* **品質管理:**
    * 運営側で記事が適切なものかを最終判断する。

## 4. マネタイズと経済圏（エコシステム）

### A. 記事執筆者への還元（レベニューシェア）
* **収益源:** サイト内の広告収入など。
* **配分ロジック:** `（全体の広告収入 - サーバー運営費 - 運営の利益α）` の残りを原資とする。
* **分配:** 各ユーザーの記事が稼いだPV（貢献度）の割合に応じて配分する。
* **狙い:** 運営と投稿者がWin-Winの関係になり、質の高い情報が集まるインセンティブを作る。

### B. 開発者からの「寄付」による広告モデル（スポンサード検索）
* **対象:** 自分のモデルを使ってもらいたい開発者。
* **仕組み:** 開発者が運営に「寄付」をすることで、対話型検索エンジンの結果において、そのモデルが優先的に表示されやすくなる。
* **条件:** ただし、ユーザーの検索意図（関連性）に合致している場合に限る（無関係なゴリ押しはしない）。

## 5. 本プロジェクトの究極的な目的
* **情報の構造化:** 人間とAIの両方が、AIモデルの進化や関係性を正しく追える環境（インフラ）を作る。
* **ニッチトップの確立:** 英語・日本語両対応の情報を整理し、既存の検索エンジンでは不可能な「痒い所に手が届く」体験を提供する。